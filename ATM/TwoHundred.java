package ATM;

public class TwoHundred extends Notes{

    public TwoHundred(String note, int count) {    // Constructor is created and initialized

        super(note, count);      // Assigns note and count from super class

    }
}
