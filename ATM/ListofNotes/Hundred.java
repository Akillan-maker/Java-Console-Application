package ATM.ListofNotes;

import ATM.Note.Notes;

public class Hundred extends Notes {

    public Hundred(String note, int count) {      // Constructor is created and initialized

        super(note, count);    // Assigns note and count from super class
    }
}